# Netlify + Next.js (App Router)
[build]
  command = "npm run build"
  publish = ".next"

[functions]
  # Gemini synthesis needs headroom beyond the default ~10s on some plans
  timeout = 26

[build.environment]
  NODE_VERSION = "20"
