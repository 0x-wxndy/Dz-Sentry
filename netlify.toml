# Netlify + Next.js (App Router)
[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  # Config values (not credentials) that appear in source / bundled output
  SECRETS_SCAN_OMIT_KEYS = "GEMINI_MODEL,LLM_PROVIDER"
